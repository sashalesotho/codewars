function findOdd(A) {
	for (let i = 0; i < A.length; i++) {
		if (A.filter(el => el === A[i]).length % 2 === 1) return A[i]
	}
 }

 console.log(findOdd([1,2,2,3,3,3,4,3,3,3,2,2,1]))
 

 /* 

 1) проходим по массиву, по каждому элементу фильтруем массив
 2) возвращаем элемент, чья длина отфильтрованного массива % 2 === 1
 doTest([7], 7);
    doTest([0], 0);
    doTest([1,1,2], 2);
    doTest([0,1,0,1,0], 0);
    doTest([1,2,2,3,3,3,4,3,3,3,2,2,1], 4);
 */